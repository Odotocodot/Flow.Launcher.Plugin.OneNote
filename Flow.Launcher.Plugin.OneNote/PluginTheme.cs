using System;

namespace Flow.Launcher.Plugin.OneNote
{
	public enum PluginTheme
	{
		System,
		Light,
		Dark,
		Color
	}
	//Plugin theme not icon theme, because a black icon is used in a light theme.
}

